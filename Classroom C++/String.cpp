#include <iostream>
#include <string>

using namespace std;

int main(){
    string message = "Let's suppose you have to print a big message and use it again and again in a code, you can call the message fucntion again and again in the code.";
    cout << message;

    return 0;
}
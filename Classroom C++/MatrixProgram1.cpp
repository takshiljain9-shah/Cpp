#include <iostream>

using namespace std;

int main (){
    int r, c;
    int x[10][10], y[10][10], sum [10][10];
    
    cout << "Enter values for rows and columns";
    cin >> r >> c;

    cout << "Enter values for first matrix";
    for(int i = 0; i <= r; i++){
        for (int j = 0; j <= c; j++){
            cin >> x[i][j];
        }
        cout << " "; 
        for 
        if [i][j]
    }
    
}